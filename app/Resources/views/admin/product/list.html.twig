{% extends "admin/layout.html.twig" %}

{% block breadcrumb %}
    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>Product list</h2>
            <ol class="breadcrumb">
                <li>
                    <a href="{{ path('admin_home') }}">Home</a>
                </li>
                <li class="active">
                    <strong>Product List</strong>
                </li>
            </ol>
        </div>
        <div class="col-lg-2">
            <div class="title-action">
                <a href="{{ path('admin_products_add') }}" class="btn btn-primary btn-sm">Create product</a>
            </div>
        </div>
    </div>
{% endblock %}

{% block wrapper_content %}
    <div class="row wrapper wrapper-content animated fadeInRight">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-title">
                    <h4>Product list</h4>
                </div>
                <div class="ibox-content">
                    <div class="project-list">
                        <table class="table table-hover">
                            <tbody>
                            {% for product in products %}
                                <tr>
                                    <td>
                                        {% if product.published %}
                                            <label class="label label-info">online</label>
                                        {% else %}
                                            <label class="label label-default">offline</label>
                                        {% endif %}
                                    </td>
                                    <td class="project-title" nowrap>
                                        <a href="{{ path('admin_product', {'id': product.id }) }}">{{ product.label }}</a>
                                        <br>
                                        <p style="width:350px;overflow: hidden; text-overflow: ellipsis;">
                                            <small>{{ product.description }}</small>
                                        </p>
                                    </td>
                                    <td nowrap>{{ product.price|number_format(2, '.') }}€</td>
                                    <td nowrap>{{ product.category.label }}</td>
                                    <td class="project-actions">
                                        {% if not product.published %}
                                            <a href="{{ path('admin_product_publish', {'id': product.id }) }}" class="btn btn-primary btn-sm">Publish</a>
                                        {% endif %}
                                        <a href="{{ path('admin_product_edit', {'id': product.id }) }}" class="btn btn-primary btn-sm">Edit product</a>
                                        <a href="{{ path('admin_product_delete', {'id': product.id }) }}" class="btn btn-danger btn-sm">Delete product</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td>No product found.</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
